/**
 * Database initialization script
 * This script runs the migrations to set up the SQLite database
 */

// Import the run-migrations script
require("./run-migrations");

console.log("Database initialization complete.");
